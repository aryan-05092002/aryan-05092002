import matplotlib.pyplot as plt
import numpy as np

# Assuming rmse_results is a dictionary containing RMSE for each model
# Example:
# rmse_results = {
#     'AR': 0.15,
#     'ARMA': 0.12,
#     'Indicator OLS': 0.10,
#     'PSF': 0.11
# }

models = list(rmse_results.keys())
rmse_values = list(rmse_results.values())

plt.figure(figsize=(8, 5))
bars = plt.bar(models, rmse_values, color='skyblue')
plt.title('RMSE Comparison Across Models')
plt.ylabel('RMSE')
plt.xticks(rotation=15)
plt.grid(axis='y', linestyle='--', alpha=0.7)

# Optional: Add RMSE values on top of bars
for bar in bars:
    height = bar.get_height()
    plt.text(bar.get_x() + bar.get_width()/2.0, height, f'{height:.3f}', ha='center', va='bottom')

plt.show()
